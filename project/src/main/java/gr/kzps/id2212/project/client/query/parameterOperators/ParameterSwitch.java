package gr.kzps.id2212.project.client.query.parameterOperators;

/**
 * A parameter can either be active (ON) or inactive (OFF)
 * @author Antonis Kouzoupis
 *
 */
public enum ParameterSwitch {
	ON, OFF
}
